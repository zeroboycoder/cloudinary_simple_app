{
   "name": "cloudinary-react-and-node",
   "version": "1.0.0",
   "description": "",
   "main": "index.js",
   "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "server": "nodemon server.js --prefix server",
      "client": "npm start --prefix frontend",
      "dev": "concurrently 'npm run server' && 'npm run client'"
   },
   "author": "",
   "license": "ISC",
   "dependencies": {
      "concurrently": "^5.2.0"
   }
}
